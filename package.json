{
  "name": "serverless-error-handler",
  "repository": {
    "type": "git",
    "url": "https://github.com/stevejay/serverless-error-handler"
  },
  "version": "1.0.1",
  "engines": {
    "node": ">=4.3"
  },
  "description": "Error handler for AWS Lambda handlers when using the serverless framework",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --growl --recursive",
    "lint": "jshint .",
    "cover": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "author": {
    "name": "Steve Johns",
    "email": "steve@stevejay.net"
  },
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.11.15",
    "istanbul": "^0.4.5",
    "jshint": "^2.9.4",
    "mocha": "^3.2.0",
    "mocha-lcov-reporter": "^1.2.0",
    "should": "^11.1.1"
  },
  "keywords": [
    "validation",
    "request"
  ]
}
